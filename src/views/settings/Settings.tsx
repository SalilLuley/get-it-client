import { Box } from "@mui/material";

export default function Settings() {
  return (
    <Box
      component="main"
      sx={{
        display: "flex",
        ml: { sm: "240px", xs: 0 },
        backgroundColor: "red",
      }}
    >
      Settings
    </Box>
  );
}
